# PROJECT-AI

이 문서는 특정 AI 도구나 실행 환경에 종속되지 않는 프로젝트 작업 지침이다.
Codex, Claude, Gemini, Copilot 또는 사람이 작업하더라도 아래 원칙을 따른다.

## 1. 프로젝트 목적

이 프로젝트의 목적은 [Battle for Wesnoth](https://github.com/wesnoth/wesnoth)의
한국어 번역을 작성하고 검토하며 유지하는 것이다.

## 2. 기본 원칙

- 원문의 의미, 맥락, 게임 플레이 정보를 우선 보존한다.
- 고유명사, 인물명, 지명, 세력명, 능력명은 일관된 표기를 사용한다.
- 인명·지명·종족명·유닛명은 한국어 음역을 우선한다. 일본어 PO가
  영어를 그대로 보존하더라도 그것만으로 한국어 영어 보존을 결정하지
  않는다. 중국어 PO는 음역 비교의 보조 자료로 사용하고,
  `References/20250322_wesnoth_한국어번역/`에 기존 한글 표기가 있으면
  가능한 한 적극적으로 활용한다. 다만 현재 영어 원문, 게임 문맥,
  구조적 표식 또는 자연스러운 한국어와 충돌하면 그대로 복사하지 않고
  근거를 남겨 조정한다.
- PO에서는 내부 고유명사를 첫 등장 여부에 따라 수동으로 병기하지 않는다.
  gettext PO만으로는 실제 게임의 노출 순서를 신뢰성 있게 알 수 없기
  때문이다. 병기 여부는 `category`로 결정하지 않고, 용어집
  각 행의 영어 원문과 한국어 번역을 직접 비교해 실제로 음차된 부분만
  `한글(English)`로 병기한다. 외부 브랜드·프로토콜·식별자는 원문을
  유지한다.
- 복합 고유명사는 전체 영어 문구를 괄호에 넣지 않는다. 번역문 안에서
  실제 고유명사 부분만 `한글(English)`로 병기한다. 예를 들어
  `Minion of Tairach`는 `타이라크(Tairach)의 졸개`, `Lady Dionli`는
  `디온리(Dionli) 부인`, `Bones of Malin Keshar`는
  `말린 케샤르(Malin Keshar)의 뼈`로 쓴다. 직함, 수식어, 일반명사,
  소유격 조사는 괄호에 넣지 않는다.
- 변수, 서식 지정자, WML 태그, 마크업, 줄바꿈 등 기능 요소를 임의로 변경하지 않는다.
- 번역문은 자연스러운 한국어를 사용하되, 원문에 없는 정보를 추가하지 않는다.
- 확신할 수 없는 내용은 추측으로 확정하지 말고 `TODO` 또는 검토 대상으로 남긴다.
- 번역과 무관한 소스 코드, 설정, 비밀 정보는 변경하거나 수집하지 않는다.

## 3. 작업 범위

주요 작업은 다음과 같다.

1. 번역 대상과 원문 버전을 확인한다.
2. `References/20250322_wesnoth_한국어번역/`의 최신 한국어 PO 자료를
   먼저 확인하고 가능한 한 활용한다. 현재 원문·문맥·구조와 충돌하면
   검토 근거를 남긴 뒤 조정한다.
3. `References/wesnothko/`의 과거 게시글과 번역 자료에서 문맥과 번역 힌트를 확인한다.
4. 영어 원문과 일본어·중국어 locale의 번역을 비교한다.
5. 문맥을 파악한 뒤 번역한다.
6. 기능 요소와 번역 품질을 검토한다.
7. 용어집 1차 검수가 끝나면 전체 `work/<버전>/ko/*.po`를 다시 읽어
   용어집 적용 여부, 고유명사 병기, 문체·조사·수식어, 문맥별 의미,
   최신 참고 번역과의 일관성을 전수 검수한다.
8. 변경 내용을 작고 검토 가능한 단위로 정리한다.

## 4. 파일 및 형식 규칙

- 번역 파일은 원본 프로젝트의 형식과 디렉터리 구조를 따른다.
- `po/`는 원본 PO 스냅샷이므로 번역 작업 중 직접 수정하지 않는다.
- 실제 번역 수정은 `work/<버전>/ko/`에서 수행한다.
- `References/20250322_wesnoth_한국어번역/`의 PO 파일은 현재 프로젝트의
  가장 중요한 한국어 참고 자료다. 모든 항목을 100% 그대로 따르는 규칙은
  아니며, 현재 원문·문맥·구조와 충돌하는 경우에는 조정할 수 있다.
- 과거 번역 자료는 `References/wesnothko/`에 보관하고 출처를 함께 기록한다.
- gettext 파일을 다룰 때 `msgid`, 주석, 참조 위치, 서식 지정자를 보존한다.
- PO의 인접한 문자열 줄(`"..."` 다음의 `"..."`)은 실제 개행이 아니라 하나의
  문자열을 나누어 표현한 것일 수 있으므로 의미를 확인한다.
- 복수형 항목은 `msgid_plural`을 보존하고 각 `msgstr[n]`을 번역한다.
  단수형만 있는 언어 설정이라도 실제 PO 형식의 인덱스를 삭제하거나
  `msgstr`로 임의 변경하지 않는다.
- 파일 인코딩은 UTF-8을 사용한다.
- 자동 생성 파일은 직접 편집하지 않고 생성 절차를 문서화한다.
- PO나 용어집을 쓰는 정규화·동기화 도구는 먼저 단독으로 완료한다.
  쓰기 도구와 테스트·`msgfmt`·감사 도구를 병렬 실행하지 않는다. 쓰는
  중간 상태를 읽으면 일시적인 PO 손상으로 잘못 판정될 수 있다.
- 파일을 이동하거나 삭제하기 전에는 영향 범위를 확인한다.
- 실제 게임에 배포할 때는 `work/<버전>/ko/*.po`를 `msgfmt --check`로
  `.mo`로 변환하고, PO 파일을 게임 설치 경로에 직접 복사하지 않는다.
- 운영체제별 게임 데이터 경로는 고정값으로 추측하지 않고 가능하면
  `wesnoth --path`로 확인한다. Mac App Store 패키지는
  `Contents/Resources/translations/ko/LC_MESSAGES` 구조를 사용한다.
- `References/wesnothko/`의 네이버 카페 HTML과 복사 자료는 공개 저장소에
  기본적으로 포함하지 않는다. 공개 자료라도 원저작자와 라이선스·재배포
  조건을 확인하기 전에는 public push 대상이라고 가정하지 않는다.
- 1.18.x 완료 tag는 모든 활성 fuzzy와 빈 번역이 없고 테스트·구조 감사·
  `msgfmt --check`가 통과한 뒤에만 만든다. 권장 형식은
  `wesnoth-1.18.x-ko.1`이며, tag와 공개 파일 목록을 검토한 뒤 push한다.
- 버전별 경로를 코드에 새로 하드코딩하지 않는다. 기본 버전은 루트
  `VERSION` 파일에서 읽고, 다른 버전은 `WESNOTH_VERSION` 환경 변수로
  지정한다. `po/<버전>/`, `work/<버전>/`, `dist/<버전>-<작업일>/` 구조를 유지한다.
- `dist/<버전>-<작업일>/`의 MO와 `PO_LAST_MODIFIED_DATE`는 Git에 포함하고,
  `LC_MESSAGES.backup-YYYYMMDD-HHMMSS/` 백업 디렉터리만 `.gitignore`로
  제외한다.
- PO를 MO로 빌드할 때 `dist/<버전>-<작업일>/ko/PO_LAST_MODIFIED_DATE`에 전체 작업
  PO 중 가장 최근 수정일을 `YYYYMMDD`로 기록한다. 게임 언어 목록의
  `한국어 (<작업버전>-<작업날짜>)` 표식은 이 값을 사용하며, MO 생성일이나
  언어 표식 스크립트 실행일을 작업일로 간주하지 않는다. 날짜 변환은
  KST/JST(UTC+9)를 사용한다.

## 5.1 버전 문서 관리

- 루트 `README.md`, `INSTALL.md`, `PROJECT-AI.md`, `work/README.md`는
  `docs/templates/`의 `.md.in` 템플릿에서 생성한다.
- 템플릿에는 `1.18.x`과 `1.18`만 사용한다.
  실제 버전은 루트 `VERSION`에서 읽고, `WESNOTH_VERSION` 환경 변수가 있으면
  그 값을 우선한다.
- 생성 문서를 직접 편집하지 않는다. 템플릿을 수정한 뒤
  `python3 tools/render_docs.py`를 실행한다.
- 변경 전에 `python3 tools/render_docs.py --check`를 실행해 생성 문서가
  템플릿과 일치하는지 확인한다.
- 버전별 PO와 작업 문서는 자동으로 복사하거나 생성하지 않는다. 새 버전의
  `po/<버전>/`, `work/<버전>/` 자료를 준비한 뒤 문서만 다시 렌더링한다.
- GitHub Actions는 템플릿 또는 `VERSION` 변경 시 렌더링 결과를 검증한다.
  버전 전환은 Actions의 수동 실행 입력이나 로컬
  `WESNOTH_VERSION=<버전> python3 tools/render_docs.py`로 수행한다.

## 5. 번역 품질 기준

- 문장 단위보다 게임 화면에서 실제로 보이는 맥락을 우선한다.
- 가능하면 `units.wesnoth.org`에서 locale별 실제 표시 맥락을 확인한다.
- 영어 원문을 의미와 구조의 기준으로 삼는다.
- 일본어(`ja`)와 중국어(`zh_CN`)는 한자어, 고유명사, UI 표현,
  placeholder 위치를 비교하는 보조 참고 자료로 사용한다.
- 네이버 카페 게시글은 과거 자료이지만 문맥, 용어, 당시 번역자의 판단을
  확인할 수 있는 중요한 보조 참고 자료로 사용한다.
- 동일한 영어 용어는 특별한 이유가 없으면 동일한 한국어 용어로 번역한다.
- `work/<버전>/glossary.tsv`에 기록된 표준 용어를 우선 사용한다.
- 용어집의 `reference_japanese`와 `reference_chinese`는 동일한 `msgid`의
  PO 번역을 기록한 참고 정보이며, 한국어 번역을 기계적으로 결정하는
  우선순위가 아니다.
- 일본어와 한국어가 모두 영어를 보존하거나, 일본어만 영어를 보존하는
  사례가 있어도 표기 일관성이 확보되었다고 간주하지 않는다. 내부
  인명·지명은 한글 표기를 검토하고, `Discord`, `Steam`, `IRC` 같은
  외부 브랜드·프로토콜·식별자는 원문 보존 대상으로 분리한다.
- 용어집에 기록된 표준 용어는 번역자가 누구든 동일하게 사용한다.
- 용어집은 반복되는 명사·동사·형용사·고유명사와 짧은 고정 UI 표현을
  관리한다. 특정 대사나 긴 설명문 전체를 용어집에 등록하지 않는다.
- `source_term`에 `^`가 있으면 먼저 그 용례가 일반 문맥 키인지 Wesnoth의
  성별 표기인지 확인한다. `feature^`, `controller^`처럼 일반 문맥 키인
  경우 앞부분은 식별자로 취급하고 뒤의 표시 문자열만 번역한다.
  `source_term`에는 비교를 위해 전체 키를 그대로 기록한다.
- `female^`, `male^`, `race^`도 gettext 문맥 식별자이므로 번역문에
  접두사를 자동으로 넣지 않는다. 원문 접두사는 `source_term`에만
  보존하고, `msgstr`에는 `^` 뒤의 표시 문자열만 기록한다. `여성`,
  `암컷`, `종족`처럼 실제 의미로 번역된 단어는 일반 한국어 표현으로
  남길 수 있지만, `여성^회복됨`처럼 문맥 접두사를 표시 문자열로
  재현하지 않는다.
- `^`가 포함된 항목은 전체 키를 하나의 독립된 항목으로 취급한다.
  문맥 접두사가 다르면 표시 문자열이 같아도 용어집 항목과 번역을 합치거나
  덮어쓰지 않는다.
- 동일한 `msgid`가 PO 주석 문맥에 따라 서로 다른 뜻으로 표시되는 경우에는
  하나의 용어집 표준값으로 강제하지 않는다. 예를 들어 `Music`은
  `[about]`에서는 크레딧 역할명인 `음악 담당`, `[column]`에서는 설정
  항목명인 `음악`이다. 이런 문맥 의존 항목은 PO에서 각각 번역하고,
  용어집에는 단일 표준값으로 등록하지 않는다.
- `yes`·`no`처럼 일본어·중국어에서도 응답을 나타내는 항목은 한국어도
  `예`·`아니오`처럼 응답 표현으로 번역한다. `있음`·`없음`은 존재 여부나
  상태값의 문맥이 명확할 때만 사용하며, 일반적인 `yes`·`no`의 표준값으로
  사용하지 않는다.
- 용어집의 `category` 열은 인명·지명·유닛명·일반 용어 등을 구분하는
  의미 분류이다. 번역 문장을 적는 칸이 아니다.
  `person_name`, `place_name`, `race_name`, `proper_noun`, `unit_name`, `faction`,
  `ability`, `contextual`, `terrain`, `attack_type`, `ui`, `command`, `term`처럼
  실제 용례를 분류한다. 인명·지명·고유명사·UI 문구를 습관적으로
  `term`으로 기록하지 않는다.
- `Run wmlindent`처럼 도구명과 동작이 결합된 짧은 UI 표현은 등록할 수
  있지만, `wmlindent`, `wmllint`, `wmlscope` 같은 도구명 자체에는
  의미를 덧붙이지 않는다.
- 과거 번역의 대안과 논쟁은 `forbidden_terms`와 참고 메모에 기록하며
  표준 번역으로 사용하지 않는다.
- `forbidden_terms`는 실제로 채택하지 않을 과거 표현·오역·대체안을
  기록하는 선택적 칸이며, 없으면 빈 칸으로 둔다. 용어집에 들어온 항목은
  별도의 승인 상태값 없이 표준 번역으로 취급한다.
- `notes`는 출처만으로 알 수 없는 번역 결정·예외·병기 근거만 기록한다.
  실제 PO 출처는 `po/<버전>/`의 locale별 디렉터리와 프로젝트 문서로
  확인하며, 용어집에 출처 경로를 반복해서 기록하지 않는다. 특별한
  판단 근거가 없으면 `notes`는 빈 칸으로 둔다.
- 숫자만 있는 항목, 기호만 있는 항목, 긴 문장·대사·설명문은 용어집에
  넣지 않는다. 게임에서 실제로 사용되는 식별자나 기호 리터럴만
  `identifier` 문맥으로 예외 기록할 수 있다.
- UI 문구는 짧고 명확하게 작성한다.
- 서사 문구는 등장인물의 말투와 분위기를 유지한다.
- 존댓말, 반말, 명령형 등 문체를 문맥에 맞게 일관되게 유지한다.
- 숫자, 단위, 색상 코드, 링크, 변수와 같은 비언어 요소를 검증한다.
- 번역 전후의 구조적 표식을 반드시 동일하게 유지한다.
  - 실제 개행 문자(`\n`)와 문단·대사 경계를 추가하거나 삭제하지 않는다.
    PO의 여러 `"..."` 줄은 파일 가독성을 위한 분할일 수 있으므로 실제
    게임 개행과 구분한다.
  - Pango/HTML 태그(`<i>`, `<b>`, `<span ...>`, `<ref ...>` 등)는 태그
    이름·속성·짝·개수를 원문과 동일하게 유지하고 태그 안의 텍스트만 번역한다.
  - `%s`, `%d`, `$unit.name`, `$gold_cost` 같은 placeholder는 철자와
    대소문자를 바꾸거나 삭제·추가하지 않는다. 한국어 조사는 placeholder
    뒤에 붙이며 placeholder 이름에 포함하지 않는다.
  - 특별한 근거가 없으면 위 요소의 순서도 원문과 동일하게 유지한다.
- manpage 번역에서는 `B<>`, `I<>` 같은 roff 표식의 이름·짝·내용을
  유지한다. 옵션명, 명령 구문, 파일명, URL, 경로, 역슬래시 같은 리터럴은
  번역하지 않고 주변 설명만 번역한다.
- `#, fuzzy`가 붙은 항목은 번역 완료로 간주하지 않는다. 번역을 검토한 뒤
  의미와 구조가 맞으면 fuzzy 표시를 제거하고, 확신할 수 없으면 검토
  상태로 남긴다.
- 번역 후 `python3 tools/audit_po_structure.py`와 전체 테스트를 실행해
  개행·태그·placeholder 보존 여부를 확인한다.

## 6. 줄바꿈 규칙

- 게임 UI의 자동 줄바꿈을 맞추기 위해 번역문에 임의의 개행을 넣지 않는다.
- 원문에 실제 문단 구분, 대사 구분 또는 `\n`이 있으면 그 의미를 보존한다.
- PO 파일에서 긴 문자열을 여러 줄의 `"..."`로 나누는 것은 가독성을 위한
  표현일 수 있으며, 게임 화면의 줄바꿈과 동일하지 않다.
- 문자열 조각의 경계에 있는 공백은 실제 번역 문자열에 포함되므로 확인한다.
  특히 둘째 줄이 공백으로 시작하거나 첫째 줄이 공백으로 끝나는 경우,
  의도한 단어 간 공백인지 검토한다.
- 개행을 넣거나 제거하면 의미와 UI 표시가 달라질 수 있으므로 원문과
  `msgid`의 개행 구조를 먼저 비교한다.

## 7. 예외 및 판단 기록

기본 규칙과 다른 처리가 필요할 때는 조용히 예외를 만들지 않는다.
다음 순서로 판단하고 근거를 문서나 용어집에 남긴다.

1. 원문 PO와 `References/20250322_wesnoth_한국어번역/`의 기존 표현을 먼저
   대조한다.
2. 기존 표현이 현재 원문, 게임 UI, 또는 문법 구조와 충돌하면 영어 원문을
   기준으로 일본어·중국어 PO와 과거 카페 자료를 추가 확인한다.
3. 표준 용어를 바꾸는 경우 기존 `forbidden_terms`와 `notes`에 변경 이유를
   기록한 뒤 관련 PO를 함께 수정한다.
4. 구조 보존 규칙을 위반해야 하는 것처럼 보이는 경우에는 번역자가
   임의로 처리하지 말고 검토 대상으로 남긴다. 원문 자체의 불균형한 태그,
   placeholder, 공백 또는 개행은 예외로 간주하지 않고 먼저 오류 가능성을
   조사한다.

다음은 허용되는 예외가 아니라 별도 검토가 필요한 사례다.

- 같은 표시 문자열이 `^` 문맥 키에 따라 다른 의미를 갖는 경우
- 인명·지명·세력명처럼 기존 한국어 자료와 현재 게임 표기가 충돌하는 경우
- 원문에 태그·placeholder·개행이 불균형하거나 잘못 생성된 것으로 보이는 경우
- 복수형, manpage roff, 명령어·경로처럼 일반 문장 규칙을 적용할 수 없는 경우
- 과거 카페 자료의 표현을 유지할지 현대적인 표현으로 바꿀지 판단이 필요한 경우

예외는 번역 파일 안의 임시 주석으로만 남기지 말고, 재현 가능한 근거와
영향 범위를 `work/README.md`, 버전별 README, 용어집 또는 검토 기록에
남긴다. 같은 유형의 예외가 반복되면 이 문서의 규칙과 테스트를 갱신한다.

## 8. 반복 작업 절차

수동 번역 배치는 기본적으로 50개 항목 단위로 진행한다. 처리 속도를
높이기 위해 임의로 10개, 20개 또는 40개 단위로 줄이지 않는다. 다만 다음과
같이 한 항목의 검토 비용이 높은 경우에는 해당 항목만 별도 소배치로
나눌 수 있다.

- 태그·placeholder·실제 `\n` 또는 복수형 구조를 함께 검토해야 하는 항목
- 인물·지명·세력명 또는 `^` 문맥 키의 의미가 충돌하는 항목
- 긴 대사·서사 문장으로 말투와 앞뒤 문맥을 확인해야 하는 항목
- 원문, 최신 한국어 참고 자료, 일본어·중국어 자료가 서로 충돌하는 항목

소배치로 나눈 경우에도 다음 배치부터 기본 50개로 복귀하며, 작업 보고에
분할 사유와 처리한 항목 수를 기록한다. 배치 크기는 품질 검토를 생략하기
위한 기준이 아니라, 검토 가능한 작업량을 정하는 운영 기준이다.

각 번역 배치는 다음 순서로 진행한다.

1. 대상 PO의 영어·일본어·중국어·최신 한국어 참고 자료를 확인한다.
2. 번역하고, 새로 반복될 용어만 용어집에 추가하거나 기존 항목을 갱신한다.
3. 용어집을 갱신했다면 관련 `work/<버전>/ko/*.po`에 표준 번역을 반영한다.
4. 문서에 새 규칙·예외·판단 기준이 생겼다면 같은 작업에서 기록한다.
5. 테스트, 구조 감사, `msgfmt --check`를 실행하고 결과와 남은 위험을 보고한다.

문서·용어집·PO 중 하나만 갱신된 상태를 완료로 보지 않는다. 단, 긴
문장이나 일회성 대사는 용어집에 넣지 않고 PO에만 기록한다.

용어집 1차 검수 후에는 전체 PO 전수 검수를 별도 작업으로 수행한다.
전수 검수에서는 모든 활성 `msgid`를 대상으로 다음을 확인한다.

- 용어집 표준 번역이 실제 PO에 일관되게 적용되었는가
- 인명·지명·종족명·세력명·유닛명의 음차와 `한글(English)` 병기가 정확한가
- 같은 단어가 문맥에 따라 달라지는 경우 올바른 의미와 문체를 사용하는가
- 일본어·중국어·최신 한국어 참고자료와 비교해 의미가 빠지거나 추가되지 않았는가
- 긴 대사와 설명문이 자연스럽고, 조사·존대·명령형이 주변 문맥과 맞는가
- `^`, 실제 `\n`, 태그, placeholder, 복수형, roff 표식이 보존되었는가
- 긴 설명문 속 고유명사도 동일한 병기 규칙을 적용했는가? 이미 전체
  이름을 병기한 문장에서는 구성요소를 다시 중첩하지 않았는가?

자동 감사가 통과해도 이 전수 검수를 생략하지 않는다. 전수 검수에서
새 표준 용어가 발견되면 용어집을 갱신한 뒤 관련 PO 전체에 다시 적용하고,
변경된 항목을 재검수한다.

## 9. 검토 체크리스트

- [ ] 원문과 번역문이 의미상 대응하는가?
- [ ] `References/20250322_wesnoth_한국어번역/`의 기존 번역을 먼저 확인했는가?
- [ ] 변수와 서식 지정자가 모두 보존되었는가?
- [ ] 복수형이면 `msgid_plural`과 모든 필요한 `msgstr[n]`을 확인했는가?
- [ ] 번역하지 않아야 할 식별자나 태그를 변경하지 않았는가?
- [ ] fuzzy 표시가 남아 있지 않거나, 아직 검토 대상으로 명시되어 있는가?
- [ ] manpage의 roff 표식과 명령·경로·URL 리터럴을 보존했는가?
- [ ] `^` 문맥 키가 있다면 `^` 앞 식별자를 번역문에 넣지 않았는가?
- [ ] `^` 문맥 키를 다른 문맥의 동일한 표시 문자열과 합치지 않았는가?
- [ ] 기존 용어와 표기가 일치하는가?
- [ ] 관련 과거 자료가 있다면 `References/wesnothko/`에서 확인했는가?
- [ ] 영어·일본어·중국어 locale과 비교할 때 `%s` 등의 placeholder 위치를 검증했는가?
- [ ] 임의의 UI 줄바꿈을 넣지 않았으며, 문자열 경계의 공백을 확인했는가?
- [ ] 필요한 경우 Units 사이트와 번역 통계에서 locale별 결과를 비교했는가?
- [ ] 문맥상 어색하거나 오해할 수 있는 표현이 없는가?
- [ ] 가능한 경우 게임 또는 gettext 도구로 결과를 확인했는가?
- [ ] 새 규칙이나 예외가 생겼다면 문서·용어집·테스트 중 알맞은 곳에
  재현 가능하게 기록했는가?

## 10. 작업 보고

작업을 마칠 때 다음 내용을 간단히 남긴다.

- 변경한 범위
- 확인한 원본 버전 또는 커밋
- 실행한 검증 명령
- 남은 TODO와 추가 검토가 필요한 항목

이 문서는 프로젝트가 성장함에 따라 갱신한다. 특정 도구의 전용 명령이나
설정은 이 문서의 필수 규칙으로 만들지 않고, 별도 문서에 기록한다.

## 11. 문서 기반 전체 감사

이 프로젝트는 이 문서와 `README.md`, `work/README.md`,
`work/<버전>/README.md`, `INSTALL.md`만 읽어도 번역 결과를 검증할 수
있어야 한다. 감사자는 과거 대화나 작업자의 기억을 전제로 하지 않는다.

### 감사 대상의 정의

- `work/<버전>/ko/*.po`가 감사 대상이다. `po/<버전>/`은 원본 스냅샷이며
  비교 기준이지 수정 대상이 아니다.
- `msgid ""`인 첫 번째 헤더 블록은 번역 메시지 수와 빈 번역 수에서
  제외한다.
- `#~`가 붙은 obsolete 항목은 현재 게임에서 사용되지 않는 과거 메시지라서
  활성 번역 진행률과 완료 기준에서 제외한다.
- `#, fuzzy`가 붙은 활성 항목은 `msgstr`가 채워져 있어도 미확정으로
  계산한다.
- 활성 비-fuzzy 항목의 `msgstr ""`와 모든 빈 `msgstr[n]`은 미번역이다.
  복수형은 하나라도 빈 인덱스가 있으면 미완료다.
- `msgfmt --check`의 `Language`, `Project-Id-Version` 같은 헤더 기본값
  경고는 PO 문법 실패와 구분한다. 종료 코드가 0이고 별도 감사 기준을
  통과하면 구조 검증은 성공하지만, 배포 전 헤더 품질은 별도로 검토한다.

감사 순서는 다음과 같다.

1. `work/<버전>/ko/`와 `po/<버전>/ko/`의 파일 목록이 같은지 확인한다.
   원본 `po/`는 수정하지 않는다.
2. 활성 PO에 빈 `msgstr`, 빈 복수형 `msgstr[n]`, `#, fuzzy`가 없는지
   확인한다. `#~` obsolete 항목은 현재 게임에서 사용되지 않으므로 활성
   번역 수에 포함하지 않는다.
3. `glossary.tsv`의 `source_term`이 중복되지 않고, 표준 번역과 모든
   작업 PO의 같은 `msgid` 번역이 일치하는지 확인한다.
4. `forbidden_terms`가 표준 번역과 같지 않은지 확인하고, 과거 오역이
   PO에 재등장하면 용어집 또는 문맥별 예외를 갱신한다.
5. `category` 열은 의미상 `category`(분류)로 해석한다. 인명·지명·실제
   개별 유닛명은 `한글(English)`로 병기하고, 일반 유닛 종류·일반
   세력명·외부 브랜드는 병기 대상이 아니다.
   외부 서비스명·폰트명·프로토콜·약어·도구명·모드 식별자는 영문 그대로
   유지할 수 있다. 영문 잔존 여부만으로 자동 병기하지 말고, 원문과
   한국어를 직접 비교해 내부 고유명사의 실제 음차 부분만 병기한다.
6. `source_term`에 `^`가 있으면 전체 키를 독립적으로 비교한다. 모든
   문맥 접두사는 번역문에 넣지 않으며, `female^`, `male^`, `race^`도
   `여성^`, `남성^`, `종족^`처럼 표시 문자열에 재현하지 않는다. 긴 문장
   안의 `^Uf`, `^Tf`, `^Efs` 같은 지형 코드는 문맥 키로 오인하지 않는다.
7. 원문과 번역문의 실제 개행, 태그, placeholder, 복수형 구조를 비교한다.
   PO 파일에서 문자열을 여러 줄로 나눈 것과 실제 `\n`을 혼동하지 않는다.
   용어집 자체는 `python3 tools/audit_glossary.py`로 중복·PO 불일치·
   금지 대안 메모 유입·병기 원문 대응을 별도로 검사한다.
   `python3 tools/audit_and_pair_embedded_names.py --check`로 활성 및
   obsolete(`#~`) 긴 문자열의 고유명사 병기 누락과 중첩 병기를 검사한다.
   obsolete 항목도 삭제하지 않고 같은 표기 규칙으로 정리한다. 병기 실행 후에는
   `python3 tools/sync_glossary_korean.py`로 용어집과 정확한 PO 항목을
   동기화한다.
8. PO를 MO로 변환하기 전에 테스트, 구조 감사, `msgfmt --check`를 모두
   실행한다. 하나라도 실패하면 완료 또는 tag 상태로 표시하지 않는다.
9. 용어집·병기 정규화는 멱등적이어야 한다. 같은 도구를 반복 실행해도
   수동 확정 번역이 되살아나거나 다른 표준값으로 바뀌지 않아야 한다.

권장 감사 명령:

```sh
VERSION="${WESNOTH_VERSION:-$(tr -d '\r\n' < VERSION)}"
python3 -m unittest discover -s tests -v
python3 tools/audit_po_completion.py
python3 tools/audit_po_structure.py
find "work/$VERSION/ko" -name '*.po' -print0 |
  xargs -0 -n1 msgfmt --check --output-file=/dev/null
```

활성 미번역과 fuzzy를 별도로 재현하려면 다음 명령을 사용한다. 출력된
`untranslated`와 `fuzzy`의 합계가 각각 0이어야 한다.

```sh
for po in "work/$VERSION/ko"/*.po; do
    untranslated="$(
        msgattrib --untranslated --no-fuzzy --no-obsolete "$po" |
        awk '$1 == "msgid" { count++ } END { print count + 0 }'
    )"
    fuzzy="$(
        msgattrib --only-fuzzy --no-obsolete "$po" |
        awk '$1 == "msgid" { count++ } END { print count + 0 }'
    )"
    printf '%s untranslated=%s fuzzy=%s\n' "$po" "$untranslated" "$fuzzy"
done
```

정상 완료 기준은 활성 fuzzy 0, 빈 번역 0, 빈 복수형 0,
`markup_mismatches=0`, `placeholder_mismatches=0`, 용어집-PO 불일치
0건이다. obsolete 항목의 fuzzy 표시는 별도로 보고하되 활성 완료 기준에는
포함하지 않는다.
