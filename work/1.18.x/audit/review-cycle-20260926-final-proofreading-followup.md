# 2026-09-26 최종 고신뢰 오탈자·띄어쓰기 후속 검수

## 범위

전체 PO의 자동 완료·구조 검증을 통과한 뒤, 규칙 위반 검색에서 발견된
활성 문장 중 원문과 대조해 확정할 수 있는 오탈자와 띄어쓰기만 수정했다.
문체 선호나 화자 말투는 이번 단계에서 일괄 변경하지 않았다.

## 수정

- `wesnoth-trow-ko.po`
  - `쓰러뜨렸어.요`, `들일쓸` 등 중복·오타를 수정했다.
  - `한 척`, `왔을 때`, `안 그렇나`, `입 벌어진`, `대기 중`을 정리했다.
  - `강화되었다`를 원문 주어와 행위에 맞는 `강화했다`로 수정했다.
  - `오크 정찰꾼`, `삼분의 일`, `명령대로`, `둘 다`, `똑똑한 줄`,
    `무한할 뿐`, `살고자 할 뿐`을 문맥과 한국어 띄어쓰기 기준에 맞췄다.
- `wesnoth-sotbe-ko.po`
  - `두 번`, `전례 없는`, `영토 내`, `둘 다`, `보는 것`, `하는 거요`,
    `생각하는 거냐`, `물리치는 걸`, `도와주는 게`를 정리했다.
  - `너희 같은 것들과는`, `원하는 거지`로 명백한 조사·띄어쓰기 오류를
    수정했다.
- `wesnoth-thot-ko.po`
  - `아동시켜라`를 `이동시켜라`로 수정하고 `둘 다`를 정리했다.
- `wesnoth-units-ko.po`, `wesnoth-utbs-ko.po`
  - `보는것은`, `보는것이`를 `보는 것은`, `보는 것이`로 수정했다.
- `wesnoth-ko.po`
  - `한두 번`과 문장부호 주변의 명백한 띄어쓰기를 정리했다.

## 검증

- 전체 활성 메시지: 21,303
- 활성 미번역: 0
- 활성 fuzzy: 0
- 구조 감사: `markup_mismatches=0`, `placeholder_mismatches=0`,
  `newline_mismatches=0`
- 용어집 감사: `rows=5035`, `errors=0`, `warnings=0`
- 전체 `msgfmt --check --check-format`: 통과
- 전체 unittest: 142개 성공
